name: Generate GitHub Profile SVG

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: SVG Profile Generator
        uses: frostyfoxie/svg-profile-generator@v1
